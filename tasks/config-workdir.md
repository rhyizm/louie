# Add workdir config to louie.config.mjs and louie should work on workdir

## Implement

User can configure working directory to louie.config.mjs to make louie work on the directory. Default should be the current directory.